services:
  remarkable-templates:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - remarkable-data:/data

# Named volume persists across container rebuilds.
# To fully reset (destroy all templates/backups): docker compose down -v
volumes:
  remarkable-data:
